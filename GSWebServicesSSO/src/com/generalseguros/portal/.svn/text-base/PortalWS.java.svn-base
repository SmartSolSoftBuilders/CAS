package com.generalseguros.portal;

import com.generalseguros.dto.Opcion;
import com.generalseguros.dto.Usuario;

/**
 * Servicio que se encargara de manipular las peticiones directamente con el portal.
 * 
 * @author Smart Solutions
 *
 */
public class PortalWS {
	
	/**
	 * Web Service que recibe los permisos y regresa un objeto JSON para que sea desplegado por el browser
	 * @param permisos
	 * @return
	 */
	public String transformaPermisosJson(String[] permisos)
	{
		permisos = new String[]{"a","b"};
		
		return "a , b";
		
	}
	
	/**
	 * 
	 * Recibe los datos del usuario y la opcion que selecciono
	 * para añadir datos en la bitacora, revisar si tiene permisos, 
	 * y regresar los datos necesarios para la redireccion o el manejo del error.
	 * 
	 * @param usuario
	 * @param opcionSistema
	 * @return la url a donde debe redireccionar el sistema, 
	 * mensaje de error si el usuario no tiene acceso
	 */
	public String procesaRedireccion(Usuario usuario, Opcion opcionSistema){
		return "";
	}
	
	/**
	 * Intenta generar contraseña nueva al usuario, y mandar un correo electronico
	 * con el resultado de la operacion 
	 * @param usuario Datos del usuario al que se le van a intentar generar las nuevas credenciales
	 */
	public void generarContrasenia(Usuario usuario){
		
	}

}
